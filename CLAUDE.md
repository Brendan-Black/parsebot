# Dependency Injection

We go hard with dependency injection. 
Any place where something can reasonably be expanded it is imperative to nip it in the bud and abstract away the details and write code using the interface


# Styling

- We use 2-space indentation
- We do not use wildcards in imports; always import all elements directly.